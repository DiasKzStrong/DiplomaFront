import { Register } from "@/components/pages/auth";
import React from "react";

const page = async () => {
  return (
    <main className="w-full h-full">
      <Register />
    </main>
  );
};

export default page;
